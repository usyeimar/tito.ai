[general]
; Configure Asterisk WebSocket client support

[pipecat]
type = websocket_client
uri = ws://tito-asterisk:8765
protocols = media
connection_type = per_call_config
connection_timeout = 1000
reconnect_interval = 1000
reconnect_attempts = 5
tls_enabled = no
